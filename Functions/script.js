// const slider = document.querySelector('input[type=range]');
// const box = document.querySelector('.box');

// slider.addEventListener('input', () => {
//   box.style.transform = `rotateZ(${slider.value}deg)`;
//   box.style.transform = `rotate3d(1,1,1,${slider.value}deg)`;
//   box.style.transform = `scale(${slider.value})`;
//   box.style.transform = `translateZ(${slider.value}px)`;
// });

// Perspective
